First Name: <%= @order.first_name %>
<h2>Flavors:</h2>
<ul>
  <% order.flavors.each do |flavor| %>
    <li><%= flavor.name %></li>
  <% end %>
</ul>
<h2>Toppings:</h2>
<ul>
  <% order.toppings.each do |topping| %>
    <li><%= topping.name %></li>
  <% end %>
</ul>

